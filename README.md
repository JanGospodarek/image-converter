1. install mongodb locally, and all required dependencies (for frontend part in frontend directory, for backend in backend dir)
2. run mongodb (on macOS: `brew services start mongodb-community@7.0`)
3. run server (run main.py script)
4. run vue app (npm run serve)
